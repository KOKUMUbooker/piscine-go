package piscine

func Split(s, sep string) []string {
	sepStr := []string{}
	sR := []rune(s)
	sepR := []rune(sep)

	sRLen := len(sR)
	sepLen := len(sepR)

	i := 0
	end := 0

	for i < sRLen {
		eOfSep := (i + sepLen) - 1
	}

	return  sepStr
}